const React = require('react');

function Ionicons({name, size, color, style}) {
  return React.createElement('Ionicons', {name, size, color, style});
}

module.exports = Ionicons;
module.exports.default = Ionicons;
